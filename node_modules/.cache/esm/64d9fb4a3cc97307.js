let Joi,mongoose,UserInputError,signUp,signIn,signOut,User,Auth;_3cc‍.x([["default",()=>_3cc‍.o]]);_3cc‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_3cc‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_3cc‍.w("apollo-server-express",[["UserInputError",["UserInputError"],function(v){UserInputError=v}]]);_3cc‍.w("../schemas",[["signUp",["signUp"],function(v){signUp=v}],["signIn",["signIn"],function(v){signIn=v}],["signOut",["signOut"],function(v){signOut=v}]]);_3cc‍.w("../models",[["User",["User"],function(v){User=v}]]);_3cc‍.w("../auth",[["*",null,function(v){Auth=v}]]);






_3cc‍.d({
  Query: {
    me: (root, args, { req }, info) => {
      // TODO: projection
      Auth.checkSignedIn(req)

      return User.findById(req.session.userId)
    },
    users: (root, args, { req }, info) => {
      // TODO: auth, projection, pagination

      Auth.checkSignedIn(req)

      return User.find({})
    },
    user: (root, { id }, { req }, info) => {
      // TODO: auth, projection, sanitization

      Auth.checkSignedIn(req)

      if (!mongoose.Types.ObjectId.isValid(id)) {
        throw new UserInputError(`${id} in not vaild user ID`)
      }

      return User.findById(id)
    }
  },
  Mutation: {
    signUp: async (root, args, { req }, info) => {
      // TODO: not auth

      Auth.checkSignedOut()

      await Joi.validate(args, signUp, { abortEarly: false })

      return User.create(args)
    },
    signIn: async (root, args, { req }, info) => {
      const { userId } = req.session

      if (userId) {
        return User.findById(userId)
      }

      await Joi.validate(args, signIn, { abortEarly: false })

      const user = Auth.attemptSignIn(args.email, args.password)

      req.session.userId = user.id

      return user
    },
    signOut: (root, args, { req, res }, info) => {
      Auth.checkSignedIn(req)

      return Auth.signOut(res, res)
    }
  }
});
